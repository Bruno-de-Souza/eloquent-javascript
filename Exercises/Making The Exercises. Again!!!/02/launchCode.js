const input = require('readline-sync');

let iniCombustivel = 0, numAstronautas = 0, altitude = 0;

while (iniCombustivel > 5000 && iniCombustivel < 30000) {
    let info = input.question("Informe o número de astronautas: ");   
}